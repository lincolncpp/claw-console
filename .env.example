VITE_GATEWAY_HOST=127.0.0.1
VITE_GATEWAY_PORT=18789
# Browser-exposed operator token. Use only on trusted admin machines and origins.
VITE_GATEWAY_TOKEN=
# Optional: set to true in local dev to log gateway frame metadata without payload bodies.
VITE_DEBUG_GATEWAY_FRAMES=false
